---
name: townwave-director
description: TownWave AI 导演操作系统（影视级写实短剧/短片一站式管线）。读取定稿剧本后，按「深度理解剧本 → 项目台词管理 → 角色/道具资产 → 场景资产管理 → 分镜剧本 → 导演镜头设计 → Loop 诊断与修复」七项能力一条流贯通，全程资产一次定稿、全片精准继承（绝不下游重新发明场景/人物/道具），支持多模型适配（MiniMax H3/Seedance/可灵/Runway/Sora/Flux/Midjourney 等，换模型只换适配不改真相源）、AI 音乐音效与逐镜自检修复。触发词：AI 导演、短剧、分镜、角色资产、场景资产、剧本转视频、首尾帧、一致性、Loop 诊断、多模型、换模型、TownWave 导演。可在聊天内直接生成或输出外部平台提示词。
---

# TownWave AI 导演操作系统（七项能力 · 环环相扣）

**版本：1.1.0**
**设计来源**：吸收 `gj-ai`（流程骨架/门禁/跨平台记号）、`longzeflow`（project 状态机 + 4 契约）、`scene-multi-angle-generator`（场景资产库+索引表）、`lingjian`（6 段式审查）、`director-master`/`Cinematic Storyboard Writer`（分镜连续性）、`short-drama-character-prompt-agent`（角色提示词引擎）等精华，重写成一条**资产精准继承**的闭环。

---

## 00 · 启动同步（每次使用先自动拉取最新版）

> **目的**：你两台电脑换着用，要保证「一打开 skill 就是 GitHub 上的最新版」，不用自己记着手动 pull。
> 本 skill 已是一个 git 仓库，真相源在 `https://github.com/GaoJiedot/townwave-director.git`。

**铁律：每次被调用时，执行任何 ①~⑦ 之前，必须先做这一步。**

1. 运行自动更新脚本（它只做 `git pull --ff-only`，**仅快进合并，绝不覆盖你本地未推送的改动**）：
   - 在终端 / 任何 shell 执行：
     ```
     cmd /c "%USERPROFILE%\.workbuddy\skills\townwave-director\auto-update.bat"
     ```
   - 找不到 git 时脚本会自动用 `gitw.bat` 定位 PortableGit，无需配 PATH。
2. 按输出决定后续：
   - 显示 `Already up to date.` → 本地已是最新，**直接继续** ①。
   - 显示拉取了若干 commits（如 `Updating xxx..yyy`）→ 告知用户：「skill 已自动更新到最新版（commit `yyy`）」；并**建议用户在本轮创作结束后重开一次会话**，让 WorkBuddy 重新加载最新 SKILL.md 指令（本轮用已加载的旧指令继续也不影响正确性）。
   - 报错 / 无网络 / 需登录 / 本地有未推送提交导致非快进 → **不阻塞**，用本地现有版本继续，并提示用户：稍后手动双击 `sync-pull.bat`，或先双击 `sync-push.bat` 把本地改动推上去再拉。
3. **这一步非阻塞、尽力而为**：网络不通、认证失败、工作区有未提交改动，都绝不允许卡住创作流程。

> **完善 skill 后的约定**：你在任一机器上改完 skill，记得双击 `sync-push.bat` 推上去；另一台机器下次调用本 skill 时，阶段 00 会自动把它拉下来。

---

## 一、核心思想（三句话）

1. **真相源唯一**：所有已定稿资产集中在 `bible.md`（= longzeflow 的 `project` 状态），下游只能引用，不能重述。
2. **环环相扣**：上游定稿的角色/场景/道具，到分镜/视频阶段**只拽 ID + 镜头语言**，绝不重新描写外貌/场景/道具视觉。
3. **哪里错修哪**：每镜生成后跑自检，只修正有问题的部分，正确的不重来。

---

## 二、七项能力 DAG（流程完全按图片来）

```text
00 启动同步         ← 每次使用先 git pull --ff-only 拉取最新版（非阻塞，失败用本地版继续）
        ↓
① 深度理解剧本      ← 读定稿剧本，提炼人物动作/台词/世界规则/情绪曲线（真相源起点）
        ↓
② 项目台词管理      ← 建 bible.md + ledger.md，全片资产集中登记（贯穿全程的"账本"）
        ↓
③ 角色/道具资产     ← 定稿 CHAR-* / PROP-*，发锁定事实（门禁：全定稿才解锁）
        ↓
④ 场景资产管理      ← 定稿 SCENE-* 四宫格 + 色卡HEX + 360°补角（空间基准包）
        ↓
⑤ 分镜剧本         ← 把①的理解锚定成镜号清单（只引用③④的ID，不重述）
        ↓
⑥ 导演镜头设计     ← 锁机位/调度/走位 + 首尾帧视频（首帧=定稿图，尾帧=分镜目标）
        ↓
⑦ Loop 诊断与修复   ← 每镜自检"是否引用未定稿资产/是否有新增"，只修错的不重来
```

---

## 三、环环相扣 · 5 条硬机制（写进每个节点的强制纪律）

| # | 机制 | 落地方式 |
|---|---|---|
| **1** | **资产 ID + 锁定事实** | ③角色/道具定稿 → `CHAR-01`（外貌/服装/状态锁定块）；④场景 → `SCENE-01`（结构/HEX/光照锁定块）；③道具 → `PROP-01`。全程只认 ID |
| **2** | **禁止撰写（No-Authoring）** | ⑤分镜/⑥视频提示词里**禁止出现任何人物外貌、场景视觉、道具外观的自由描述文字**。这类事实只存在于 `bible.md` 锁定块，下游以「引用区」原样粘贴，**AI 不得撰写、不得改写、不得精简**。AI 在⑤⑥阶段只能编写「镜头语言区」 |
| **3** | **bible 继承闸门** | 每个节点**第一步先读 bible.md**（已定稿资产全在这），**最后一步回写**新定稿资产。分镜启动时 bible 已躺着全部锁定事实 |
| **4** | **两区制模板（引用区 + 镜头语言区）** | 分镜/视频提示词 = `[引用区：从 bible 原样粘贴 CHAR-/SCENE-/PROP- 锁定块，AI 不可编辑] + [镜头语言区：景别/机位/运动/时长/动作/声音/连续性锚点] + [首尾帧边界] + [负面约束]` |
| **5** | **新增自检（生成后扫描）** | 每出一份⑤⑥提示词，只对「镜头语言区」做"新增检测"：扫描是否出现 bible 未登记的新场景名/人物名/道具视觉词，或任何外貌/场景描写句式。命中即判失败打回重做 |

> **针对你踩过的坑（分镜里又出新场景/新人物导致前后矛盾）**：你之前翻车的根因是——角色/场景已在③/④定稿，但⑤⑥提示词里 AI 又**重新描写了一遍**，而这一遍和定稿对不上，于是前后生成的东西互相矛盾。
>
> **本 skill 的物理解法**：分镜/视频提示词里**根本不写外貌和场景描写**，只写"引用谁（CHAR-/SCENE-/PROP- 的锁定块）+ 怎么拍（镜头语言）"。描写文字 100% 来自 bible，AI 在下游无权撰写，从根上消灭"两遍描述对不上"的可能。

### 两区制 · 强制模板（模块⑤⑥ 通用）

```
═══ 引用区（只读·从 bible.md 原样粘贴·AI 严禁改写）═══
CHAR-01 → [bible.characters[0].locked_facts 全文粘贴]
SCENE-03 → [bible.scenes[2].locked_facts 全文粘贴]
PROP-02  → [bible.props[1].locked_facts 全文粘贴]
═════════════════════════════════════════════════════

═══ 镜头语言区（仅此区由 AI 编写）═══
镜号 / 景别 / 机位 / 运动 / 时长 / 动作(仅运动走位) / 声音 / 连续性锚点
════════════════════════════════════

首尾帧边界（仅⑥视频）：
首帧 = 引用区 CHAR-/SCENE- 定稿图
尾帧 = 本镜分镜目标

负面约束：[继承角色/场景引擎的绝对禁令]
```

> **反面案例（禁止）**：`一个中年男人在昏暗的客厅里坐下` —— 这是 AI 在撰写外貌/场景，会漂。
> **正面案例（正确）**：`引用区 CHAR-01 + SCENE-03 ｜ 镜头语言：中景，机位微推，5s，主角走向沙发落座，环境音沿用 SCENE-03 锁定光照` —— 描写全部来自引用区，镜头语言只管"怎么拍"。

---

## 四、全局契约（沿用 longzeflow 四契约，全程有效）

- **契约 A — 风格锁定**：②确认 `style_prefix`（风格前缀）后，所有后续图片/视频提示词**必须以 `style_prefix` 开头**。改风格须明确告知影响范围并确认。
- **契约 B — 阶段关卡**：每个模块结束后展示完整产出 → 针对最易分歧点问一个问题 → **等用户明确确认**才进下一模块（沿用体验版门禁）。
- **契约 C — 生成即登记**：每张图/视频定稿后立刻回写 `bible.md` 与 `ledger.md`，不事后补。
- **契约 D — 只处理定稿**：不改戏、不加角色、不加台词、不改结局；发现冲突资产指出并等拍板。

---

## 五、状态模型（project state，落地为 bible.md）

```
project = {
  "scale": "竖屏短片" | "标准短视频" | "微电影",
  "style_prefix": "...",                       # 契约A锁定的风格前缀
  "img_model": "workbuddy-imagen",             # 模块②确认的生图模型 id（见 model_adapters.md）
  "vid_model": "minimax-h3",                   # 模块②确认的视频模型 id
  "characters": [{"id":"CHAR-01","name","description","prompt","image_url","states":[]}],
  "props":      [{"id":"PROP-01","name","description","prompt","image_url"}],
  "scenes":     [{"id":"SCENE-01","name","setting","prompt","image_url","bible":{空间/光照/色彩},"index":{镜头类型→参考图}}],
  "shots":      [{"index","scene_id","duration","description","image_prompt","video_prompt","image_url","video_url"}],
  "sound":      [{"type":"dialogue|bgm|sfx","asset","url"}]
}
```

规模参考：竖屏短片 30-60s/2-3场景/1-2角色/8-15镜；标准短视频 1-3min/3-5场景/2-4角色/15-25镜；微电影 3min+/5-8场景/3-6角色/25-50镜。

---

## 六、模型适配层（换模型只换适配，不改真相源）

> 你大概率不会全程只用同一个模型（如《结婚照》用 MiniMax H3，其他用 Seedance/可灵/Sora/Flux…）。本层让**同一份资产**按不同模型语法/参数/能力自适应生成。完整注册表见 `references/model_adapters.md`。

- **规范形式（canonical）**：所有提示词先以两区制 + 首尾帧 + 负向约束写成统一结构，生成时再翻译。
- **两类模型**：
  - **R 类（参考图/首尾帧）**：描写由定稿图锚定，提示词只写镜头语言（引用区粘贴 ID）。如 Seedance、可灵、Runway、MiniMax H3、即梦、WorkBuddy 生图/视频。
  - **T 类（纯文字）**：无图输入，必须把 bible 锁定块**原样内联成文字**进提示词（仍是 bible 来的、非 AI 现编）。如 Sora 纯文字模式。
  - 无论哪类，"描写来自 bible、AI 不现编"铁律不变；机制 5 自检对两者都生效。
- **选择时机**：模块②与 `style_prefix` 同时确认 `img_model` / `vid_model`（写进 bible）；任何单镜可在门禁处临时改模型。
- **未登记模型**：先按规范形式输出 + 标注「⚠ 需手动核对参数」，跑通后回填 `model_adapters.md`。

---

## 七、统一生成门禁（每个出图/出视频环节必问，不得静默自动出）

1. 先按 `bible.img_model`/`vid_model` 选适配器，把两区制提示词**翻译成该模型语法**（含锁定资产引用、尺寸、HEX、负向约束、首/末帧或内联描写）。
2. 展示本环节**适配后完整提示词**。
3. 显式二选一：【A】我直接调用 `IMG`/`VID` 生成；【B】你拿提示词自己生成。
4. 你明确选 A/B 后才执行；回传结果不算定稿，须明确确认才作下游依赖。

---

## 八、七项能力 · 节点脚本

### ① 深度理解剧本
- **输入**：定稿剧本全文（契约 D：不改戏/不加角色/不改台词/不改结局）。
- **铁律**：只**提取**不**脑补**——所有解析项须能在剧本原文定位，否则标「待确认」。
- **三步流程**（已下沉为 `references/understanding_engine.md`）：
  1. **结构化解析**：输出七项——梗概 / 人物表(身份·关系·动作线) / 空间链 / 状态链 / 世界规则 / 台词与潜台词要点 / 视觉母题。人物只提身份动作线、场景只提空间名+功能，**不写外貌与视觉描写**（那分别是③④的活）。
  2. **情绪曲线图**：16:9 出图，标起势/峰值/转折/低谷/落点节点（全片节奏地图，下游③.5④⑤.5 对照）。
  3. **交付建账本输入**：把人物/场景/道具**空壳** + `style_prefix` 候选 + 情绪曲线节点，作为②初始化 `bible.md` 的填入内容（外貌/视觉留③④填）。
- **门禁**：展示解析 + 情绪曲线图 → 问「确认/修改/补哪些待确认项」→ 确认后交付②建账本，进②。
- **参考引擎**：`references/understanding_engine.md`（已下沉 longzeflow Stage1-2 + character-extractor + gj-ai 十项解析 + 16:9 情绪曲线图）。

### ② 项目台词管理（建账本 + 锁模型）
- **做法**：初始化 `bible.md`（按第五节状态模型）+ `ledger.md`（连续性台账模板见 `templates/`）。把①的解析登记进 bible：角色/场景/道具/声音四大资产表先建空壳与 ID 规划。
- **锁模型（关键新增）**：与 `style_prefix` 同时确认 `img_model` / `vid_model`：
  - 从 `references/model_adapters.md` 给候选（你高频：图片 `nanobanana` / `gpt-image`(image2)，视频 `minimax-h3` / `seedance`），你拍板写进 bible。
  - 示例：`img_model=nanobanana`，`vid_model=minimax-h3`（或某镜临时切 `seedance` 做首尾帧连续）。
  - 单镜可在门禁处临时换模型（如某镜改用 Seedance 做首尾帧连续）。
- **产出**：bible.md（含 style_prefix + img_model + vid_model）、ledger.md。
- **说明**：`style_prefix` 在①解析后由你推荐 1-2 个方向并确认（契约 A 锁定），写入 bible。
- **门禁**：展示 bible 骨架（含模型选择）→ 确认 → 进③。
- **参考引擎**：`references/model_adapters.md`（模型适配层）。

### ③ 角色 / 道具资产
- **输入**：bible 中角色/道具空壳 + ①解析。
- **做法**：
  - 角色：用 `references/character_engine.md` 的提示词引擎，输出 9:16 母板提示词（含负向约束/参数/绝对禁令/去撞脸维度）。逐角色生成→确认→发 `CHAR-0X` + 锁定事实块。血缘/换装/状态基于定稿图生成。
  - 道具：3:4 产品档案照（不出现人物/手），受控补全标注「补全」。
- **硬门禁（强制暂停）**：全部 `CHAR-*`/`PROP-*` 定稿后才解锁④；未确认不得进场景。
- **回写**：每个定稿资产立刻写回 bible.characters / bible.props。
- **参考引擎**：`references/character_engine.md`（已下沉 short-drama-character-prompt-agent + character-model-sheet + character-consistency-helper 精华）。

### ④ 场景资产管理
- **输入**：bible 中场景空壳 + ①空间链 + style_prefix。
- **做法**：用 `references/scene_engine.md`：
  1. 每场景四宫格/多角度参考图（N/S/E/W 绝对坐标系）。
  2. 场景圣经（空间尺寸/固定道具/可变道具/光照/色彩基调）。
  3. **镜头索引表**（镜头类型→相机方向→参考图 ID），使⑤分镜自动引用正确参考。
  4. 环境色卡 HEX（主色/辅助色/冷暖/强调色/肤色保护/禁用色）。
  5. 关键场景 360° 补角（长镜头/转身/跨轴）。
- **硬门禁**：全部 `SCENE-*` 定稿后才进⑤。
- **回写**：bible.scenes（含 bible 块 + index 表）。
- **参考引擎**：`references/scene_engine.md`（已下沉 scene-multi-angle-generator + bootstrap-locations + vr-panorama 精华）。

### ⑤ 分镜剧本
- **输入**：bible（角色/场景/道具全定稿）+ ①情绪曲线。
- **关键纪律（针对你的痛点）**：**分镜不撰写任何人物外貌 / 场景视觉 / 道具外观描写**。分镜里的人物/场景/道具一律以 `CHAR-0X`/`SCENE-0X`/`PROP-0X` 引用 bible 锁定块；分镜只规定"怎么拍"（镜头语言），不规定"长什么样"。
- **做法**：
  1. 导演拆段 → 支线分析 → 分镜清单（镜号/景别/机位/运动/时长/声音/连续性锚点）。
  2. **两区制输出**：每镜提示词 = `引用区`（从 bible 原样粘贴对应锁定块）+ `镜头语言区`（景别/机位/运动/时长/动作走位/声音/连续性锚点）。**镜头语言区禁止出现外貌/场景描写句式**。
  3. 关键镜头出故事板速写（按统一门禁，速写只画机位调度，不重画角色/场景外观）。
- **新增自检（机制5）**：仅扫描「镜头语言区」——若出现未在 bible 登记的场景名/人物名/道具视觉词，或任何外貌/场景描写句式，判失败，要求改为引用区后重出。
- **门禁**：展示分镜清单（引用区 + 镜头语言区两栏）→ 确认 → 冻结镜头设计，进⑥。
- **参考引擎**：`references/storyboard_engine.md`（已下沉 director-master 九列分镜表 + Cinematic Storyboard Writer 四大连续性 + 视听语言四维编码精神 + 两区制模板）。

### ⑥ 导演镜头设计（首尾帧视频）
- **输入**：bible（全定稿）+ ⑤分镜清单（两区制）。
- **关键纪律**：视频提示词同样**只由「引用区 + 镜头语言区」组成**。人物外观 = 引用区 `CHAR-0X` 锁定块锚定；场景 = 引用区 `SCENE-0X` 锁定块 + 索引表；**禁止任何新描写**。
- **做法**：
  1. 每镜「首帧（引用区 `CHAR-0X`/`SCENE-0X` 定稿图）+ 尾帧（分镜目标）」为时间边界派生视频提示词。
  2. **两区制输出**：引用区原样粘贴 → 镜头语言区只写运镜/时长/动作/声音；HEX 与索引表从引用区继承。
  3. 对白镜传入声音预制作音频做口型同步（Seedance 能力）。
  4. **新增自检（机制5）**：仅扫描「镜头语言区」是否出现新场景/人物/道具描写，命中即打回。
  5. 逐镜生成防覆盖（同名互盖，文件名显式加镜号）。
- **门禁**：逐镜确认 → 进⑦。
- **参考引擎**：`references/director_engine.md`（已下沉 anchor-scene-generator 首尾帧 + seedance-prompt-template 运镜词库 + extract-last-frame + 两区制模板）。

### ⑦ Loop 诊断与修复
- **输入**：⑥全部镜头片段 + ledger.md。
- **做法**：用 `references/diagnostic_engine.md`：
  1. **6 段式审查**（风格/角色/场景/参考图一致/参考图分类/镜头描述），输出问题表（错误/警告/提示）+ 修改建议。
  2. **返工规则**：失败要诊断、不要重抽——只修问题镜，正确的不重来。
  3. **IP/合规规避**：隐性 IP 打散、连字误杀规避（如"仓库里"→"仓库内"）。
  4. **连续性台账复核**：每镜向上追溯资产版本，找漂移/穿帮。
- **门禁**：展示诊断报告 → 你确认修复范围 → 只修指定镜 → 复检 → 交付。
- **参考引擎**：`references/diagnostic_engine.md`（已下沉 lingjian 6 段审查 + ai-video-storyboard-framework 返工规则 + seedance-compliance-director 合规精华）。

---

## 九、已知边界
- 音频 TTS/真人配音、BGM 生成依赖外部或后续接入；当前产出完整提示词 + EDL + 资产清单，待工具可用直投。
- 群演/背景一致性、表演跨镜连续性为后续扩展项。
- 版权与合规按项目外部处理。

---

## 十、与本仓库其他 skill 的关系
- 本 skill 是**总装操作系统**；`references/*_engine.md` 是各模块的"提示词引擎"。
- 散装集合包（`short-drama-ai-skills-main`）中约 15 个黄金文件已被吸收进本 skill 与各 engine，其余 240+ 冗余/图库/套话文件**不纳入**。
